# Start the demo

```bash
cd /Users/shuken/AI/dispered/demo
claude
```

Approve the `nightshift` MCP server when prompted. Then ask in plain English —
"show me the corpus", "run the burst", "what did my agent screw up?" — or drive
the CLI directly with `/Users/shuken/AI/dispered/ns <command>`.

Full context is in `CLAUDE.md`, which Claude reads automatically.
